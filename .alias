
# =====================================================================================
# General
# =====================================================================================
alias cp 'cp -i'
alias mv 'mv -i'
alias rm 'rm -i'
alias h 'history'

# $ find . -name '*.c' -print  # means find all *.c files

alias cd.. 'cd ..'
alias c 'cd ..'

alias ls 'ls -aF'  # -a: display conceal files
alias l 'ls -ls'  # -s: display size
alias ll 'ls -lsrt'

alias vim 'vim -p'
alias gvim 'gvim -p'
alias vi 'gvim -p'

# homebrew
alias brew_upgrade 'brew upgrade cscope;\
                    brew upgrade curl;\
                    brew upgrade git;\
                    brew upgrade gnupg;\
                    brew upgrade macvim;\
                    brew upgrade pkg-config;\
                    brew upgrade readline;\
                    brew upgrade repo;\
                    brew upgrade sqlite;\
                    brew upgrade subversion;\
                    brew upgrade wget;\
                    '

# =====================================================================================
# Realtek
# =====================================================================================
alias rtk_svn_update 'cd $REALTEK/SVN/risc_doc/         ; echo Updating risc_doc...         ; svn up * ; echo ------- ;\
                      cd $REALTEK/SVN/rlx_doc/          ; echo Updating rlx_doc...          ; svn up * ; echo ------- ;\
                      cd $REALTEK/SVN/platform/trunk/   ; echo Updating platform/trunk...   ; svn up * ; echo ------- ;\
                      cd $REALTEK/SVN/taroko/trunk/     ; echo Updating taroko/trunk...     ; svn up * ; echo ------- ;\
                      cd $REALTEK/SVN/taroko/trunk/doc/ ; echo Updating taroko/trunk/doc... ; svn up * ; echo ------- ;\
                      cd $REALTEK/SVN/                  ; echo SVN Update finished ; echo Thank you!'
alias rtk_repo_sync 'cd $REALTEK/report_dir/software/  ; echo Updating Software Report...  ; repo sync ; echo ------- ;\
                     cd $REALTEK/report_dir/platform/  ; echo Updating Platform Report...  ; repo sync ; echo ------- ;\
                     cd $REALTEK/report_dir/processor/ ; echo Updating Processor Report... ; repo sync ; echo ------- ;\
                     echo Repo Sync finished ; echo Thank you!'
